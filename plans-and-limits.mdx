---
title: "Plans & Limits"
description: "Understand rate limits, quotas, and plan tiers for the NinjaChat API."
---

API access requires a paid NinjaChat subscription. Your plan determines your rate limit and monthly quota.

## Plan tiers

| NinjaChat Plan | API Tier | Monthly Requests | Rate Limit |
|---|---|---|---|
| Starter ($11/mo) | Starter | 5,000 | 30 req/min |
| Pro ($24/mo) | Pro | 50,000 | 60 req/min |
| Expert ($50/mo) | Enterprise | 500,000 | 120 req/min |

<Note>
  Image generation uses **half** your rate limit (minimum 5 req/min). For example, the Pro plan gets 30 req/min for images.
</Note>

## Tracking your usage

Every API response includes your remaining quota in the `quota` or `usage` field:

```json
{
  "quota": {
    "remaining": 4999,
    "limit": 5000,
    "plan": "starter"
  }
}
```

## Rate limit headers

All responses include these headers so you can monitor your rate limit status:

| Header | Description |
|---|---|
| `X-RateLimit-Limit` | Max requests allowed in the current window |
| `X-RateLimit-Remaining` | Requests remaining in the current window |
| `X-RateLimit-Reset` | Seconds until the rate limit window resets |
| `Retry-After` | Seconds to wait before retrying (only on 429 responses) |

## Handling rate limits

When you exceed your rate limit, the API returns a `429` status code. Use the `Retry-After` header to determine when to retry:

```python
import time
import requests

response = requests.post("https://ninjachat.ai/api/v1/chat", ...)

if response.status_code == 429:
    retry_after = int(response.headers.get("Retry-After", 60))
    time.sleep(retry_after)
    # Retry the request
```

For production applications, implement exponential backoff to handle rate limits gracefully.
