---
title: "Quick Start"
description: "From zero to working API call in 60 seconds."
---

## 1. Get an API key

Go to [Settings → API](https://www.ninjachat.ai/settings?tab=api):
1. Pick a credit pack ($10, $25, or $100) — $10 gets you ~1,600 chat requests
2. Click **Create API Key**, give it a name, and copy it immediately (starts with `nj_sk_`, shown once)

No subscription required. Credits never expire.

## 2. Make a request

<CodeGroup>

```bash cURL
curl -X POST https://ninjachat.ai/api/v1/chat \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer nj_sk_YOUR_API_KEY" \
  -d '{
    "model": "gpt-5",
    "messages": [{"role": "user", "content": "What is the capital of France?"}]
  }'
```

```python Python (no dependencies except requests)
import requests

r = requests.post("https://ninjachat.ai/api/v1/chat",
    headers={"Authorization": "Bearer nj_sk_YOUR_API_KEY"},
    json={
        "model": "gpt-5",
        "messages": [{"role": "user", "content": "What is the capital of France?"}]
    }
)
print(r.json()["message"]["content"])
```

```javascript Node.js (zero dependencies)
const r = await fetch("https://ninjachat.ai/api/v1/chat", {
  method: "POST",
  headers: {
    "Content-Type": "application/json",
    "Authorization": "Bearer nj_sk_YOUR_API_KEY",
  },
  body: JSON.stringify({
    model: "gpt-5",
    messages: [{ role: "user", content: "What is the capital of France?" }],
  }),
});
const data = await r.json();
console.log(data.message.content);
```

</CodeGroup>

## 3. See the response

```json
{
  "model": "gpt-5",
  "message": {
    "role": "assistant",
    "content": "The capital of France is Paris."
  },
  "usage": {
    "prompt_tokens": 14,
    "completion_tokens": 8,
    "total_tokens": 22
  },
  "cost": {
    "this_request": "$0.006"
  },
  "metadata": {
    "latency_ms": 842
  }
}
```

That's it. You just called GPT-5 for $0.006.

---

## Switch models in one line

Same endpoint, same code. Just change `"model"`:

```python
# GPT-5 — general purpose ($0.006/req)
{"model": "gpt-5", "messages": [...]}

# Claude Sonnet 4.6 — best for code ($0.015/req)
{"model": "claude-sonnet-4.6", "messages": [...]}

# DeepSeek V3 — cheapest ($0.003/req)
{"model": "deepseek-v3", "messages": [...]}

# Gemini 3.1 Pro — advanced reasoning ($0.006/req)
{"model": "gemini-3.1-pro", "messages": [...]}
```

See all [20+ chat models, 7 image models, and 6 video models →](/models)

---

## Try every endpoint

<CodeGroup>

```python Chat ($0.003–$0.015/req)
r = requests.post("https://ninjachat.ai/api/v1/chat",
    headers={"Authorization": "Bearer nj_sk_YOUR_API_KEY"},
    json={
        "model": "gpt-5",
        "messages": [{"role": "user", "content": "Hello!"}]
    }
)
print(r.json()["message"]["content"])
```

```python Images ($0.03–$0.10/img)
r = requests.post("https://ninjachat.ai/api/v1/images",
    headers={"Authorization": "Bearer nj_sk_YOUR_API_KEY"},
    json={
        "model": "flux-schnell",
        "prompt": "A cute robot drinking coffee, cartoon style"
    }
)
print(r.json()["images"][0]["url"])
```

```python Video ($3.00–$5.00/vid — async)
# Step 1: Submit
r = requests.post("https://ninjachat.ai/api/v1/video",
    headers={"Authorization": "Bearer nj_sk_YOUR_API_KEY"},
    json={
        "model": "google-veo-2",
        "prompt": "A timelapse of a flower blooming",
        "duration": 5
    }
)
request_id = r.json()["request_id"]

# Step 2: Poll every 10s
import time
while True:
    s = requests.get(
        f"https://ninjachat.ai/api/v1/video/status?request_id={request_id}",
        headers={"Authorization": "Bearer nj_sk_YOUR_API_KEY"}
    ).json()
    if s["status"] == "completed":
        print(s["result"]["video_url"])
        break
    if s["status"] == "failed":
        print("Failed:", s.get("error"))
        break
    time.sleep(10)
```

```python Search ($0.05/query)
r = requests.post("https://ninjachat.ai/api/v1/search",
    headers={"Authorization": "Bearer nj_sk_YOUR_API_KEY"},
    json={
        "query": "latest AI news",
        "include_answer": True
    }
)
print(r.json()["answer"])
```

</CodeGroup>

---

## Next steps

| I want to... | Go here |
|---|---|
| See all available models | [Models](/models) |
| Understand pricing | [Pricing](/pricing) |
| Generate images | [Image Generation guide](/image-generation) |
| Generate videos | [Video Generation guide](/video-generation) |
| Use web search | [Search guide](/search) |
| Handle errors properly | [Error Handling](/error-handling) |
| Test without code | [API Playground](https://www.ninjachat.ai/api-playground) |
