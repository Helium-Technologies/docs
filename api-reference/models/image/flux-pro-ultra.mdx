---
title: "Flux Pro Ultra"
sidebarTitle: "Flux Pro Ultra"
description: "flux-pro-ultra — $0.10/img. Highest quality."
---

| | |
|---|---|
| **Model ID** | `flux-pro-ultra` |
| **Cost** | $0.10/img |
| **Speed** | ~8s |
| **Best for** | Highest quality |

## Example

```bash
curl -X POST https://ninjachat.ai/api/v1/images \
  -H "Authorization: Bearer nj_sk_YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model": "flux-pro-ultra", "prompt": "A sunset over mountains, photorealistic"}'
```

```python
import requests, os

r = requests.post("https://ninjachat.ai/api/v1/images",
    headers={"Authorization": f"Bearer {os.environ['NINJACHAT_API_KEY']}"},
    json={"model": "flux-pro-ultra", "prompt": "A sunset over mountains, photorealistic"}
)
print(r.json()["images"][0]["url"])
```
