---
title: "Flux Schnell"
sidebarTitle: "Flux Schnell"
description: "flux-schnell — $0.03/img. Fastest, batch jobs."
---

| | |
|---|---|
| **Model ID** | `flux-schnell` |
| **Cost** | $0.03/img |
| **Speed** | <2s |
| **Best for** | Fastest, batch jobs |

## Example

```bash
curl -X POST https://ninjachat.ai/api/v1/images \
  -H "Authorization: Bearer nj_sk_YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"model": "flux-schnell", "prompt": "A sunset over mountains, photorealistic"}'
```

```python
import requests, os

r = requests.post("https://ninjachat.ai/api/v1/images",
    headers={"Authorization": f"Bearer {os.environ['NINJACHAT_API_KEY']}"},
    json={"model": "flux-schnell", "prompt": "A sunset over mountains, photorealistic"}
)
print(r.json()["images"][0]["url"])
```
